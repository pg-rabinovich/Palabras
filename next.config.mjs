/** @type {import('next').NextConfig} */
const nextConfig = {
  // Oculta el indicador de desarrollo de Next (la "N" abajo a la izquierda).
  // Solo afecta a local; en produccion no aparece de todos modos.
  devIndicators: false,
}

export default nextConfig
